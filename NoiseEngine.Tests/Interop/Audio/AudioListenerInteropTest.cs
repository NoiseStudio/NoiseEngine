using NoiseEngine.Audio;
using NoiseEngine.Interop.Audio;

namespace NoiseEngine.Tests.Interop.Audio;

public class AudioListenerInteropTest
{
    [Fact]
    public void Test()
    {
        AudioListener listener = new AudioListener();
    }
}
